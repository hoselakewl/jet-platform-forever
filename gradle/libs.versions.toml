[versions]
observation = "3.4.3"
letter = "1.6.7"

[libraries]
observation = { module = "io.ktor:ktor-client-content-negotiation", version.ref = "observation" }
letter = { module = "io.ktor:ktor-server-call-logging", version.ref = "letter" }
